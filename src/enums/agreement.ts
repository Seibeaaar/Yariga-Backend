export enum AGREEMENT_TYPE {
  Sale = "sale",
  Rent = "rent",
}

export enum AGREEMENT_STATUS {
  Pending = "pending",
  Declined = "declined",
  Changed = "changed",
  Settled = "settled",
  Completed = "completed",
}
